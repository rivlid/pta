---
- name: Configure Debian servers
  hosts: all

  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Install base packages
      apt:
        name:
          - vim
          - htop
          - curl
          - wget
          - git
        state: present
...